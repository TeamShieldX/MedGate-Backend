-- MedGate Database Schema Placeholder
-- Real schema to be created during Synthea Data Integration (SHI-6)

-- Table structure preview:
-- CREATE TABLE users (id SERIAL PRIMARY KEY, username VARCHAR(50), role VARCHAR(30));
-- CREATE TABLE patients (id UUID PRIMARY KEY, first_name VARCHAR(50), last_name VARCHAR(50), gender VARCHAR(10), birth_date DATE);
-- CREATE TABLE audit_logs (id SERIALPRIMARY KEY, user_id INT, action VARCHAR(50), resource VARCHAR(50), result VARCHAR(20), reason TEXT, timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
